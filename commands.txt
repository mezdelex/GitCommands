Create sub-branch   => git checkout -b <child_branch> <father_branch>

Add and commit      => git commit -am "Message"

Merge sub-branch    => git checkout <father_branch>
                       git merge --no-ff <child_branch>

Push both branches  => git push origin <father_branch>
                       git push origin <child_branch>
